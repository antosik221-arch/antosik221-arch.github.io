* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: 'Arial', sans-serif; background: #121416; color: #f0f0f0; line-height: 1.6; }
.container { width: 85%; max-width: 1200px; margin: auto; }

header { background: #1c1f22; padding: 20px 0; border-bottom: 3px solid #007bff; position: sticky; top: 0; }
header .logo { float: left; font-weight: bold; font-size: 1.4rem; color: #fff; }
header nav { float: right; }
header ul { list-style: none; }
header li { display: inline; margin-left: 20px; }
header a { color: #fff; text-decoration: none; font-size: 0.9rem; text-transform: uppercase; }

.hero { height: 400px; display: flex; align-items: center; justify-content: center; text-align: center; background: #181b1e; }
.hero h1 { font-size: 2.5rem; color: #fff; margin-bottom: 10px; }

.section { padding: 60px 0; }
.section h2 { text-align: center; margin-bottom: 40px; border-bottom: 2px solid #333; padding-bottom: 10px; }

.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; }
.card { background: #1c1f22; padding: 25px; border-radius: 4px; border-top: 4px solid #007bff; }

footer { background: #0b0d0e; padding: 30px 0; text-align: center; color: #888; border-top: 1px solid #222; }
